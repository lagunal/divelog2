import 'package:flutter/material.dart';

class DivesScreen extends StatelessWidget {
  const DivesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Dives Screen'));
  }
}
